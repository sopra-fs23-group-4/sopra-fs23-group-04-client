import { Title } from "@mantine/core";
import BaseContainer from "../ui/BaseContainer";

const Rules = () => {
    return (
        <BaseContainer>
            <Title color="white"> Rules </Title>
        </BaseContainer>
    );
};

export default Rules;
